# Sprint 30c: 论文库真删除
# 对应模块: paper-library.js, lib.rs (delete_paper / delete_paper_domain),
#           paper_import/index.rs (remove)
#
# 背景（用户定调）：资源管理至少要有 C 和 D。课程删除是软删（只不展示、
# 不动磁盘文件）——保持现状即可；但论文/论文集的删除必须是真删除，
# 文件本身也要被删掉。

Feature: 论文库真删除

  作为管理论文库的用户
  我想要删除论文或整个领域时磁盘文件一并删除
  以便论文库不会积攒看不见的垃圾文件

  Background:
    Given 论文库中有“铝电解”领域两篇真实文件论文

  Scenario: 删除单篇论文（文件与索引一并删除）
    When 用户查看论文导读欢迎页
    And 用户展开“铝电解”领域
    And 用户确认后点击第 1 篇论文的删除按钮
    Then 该论文的磁盘文件被删除
    And 论文库中该论文条目消失

  Scenario: 删除需确认，取消则不删
    When 用户查看论文导读欢迎页
    And 用户展开“铝电解”领域
    And 用户取消确认后点击第 1 篇论文的删除按钮
    Then 该论文的磁盘文件仍存在
    And 论文库中该论文条目仍在

  Scenario: 删除整个领域（论文集）
    When 用户查看论文导读欢迎页
    And 用户确认后删除“铝电解”领域
    Then 该领域所有论文的磁盘文件被删除
    And 论文库中“铝电解”领域卡片消失
