# Sprint 30: 论文库首页 + 缓存交互修复
# 对应模块: paper-library.js (新增), paper-search.js, settings-panel.js,
#           paper_import/index.rs (domain 字段), lib.rs (get_papers_dir / open_folder)
#
# 背景（用户实机反馈的 4 个交互问题）：
#   1. 缓存无 loading 感知——进度槽在结果列表之后，视口外；按钮无忙态
#   2. 没有打开本地地址的快捷方式
#   3. 默认保存地址用户不知道（设置里只有"默认（应用数据目录）"）
#   4. 重进论文陪读恢复上次搜索结果不对——应像课程选择一样按领域选已存论文

Feature: 论文库首页与缓存交互

  作为按领域积累论文的用户
  我想要重进论文导读时按领域看到已缓存的论文库
  以便像选课程一样直接进入阅读，而不是面对上次搜索的残留

  Scenario: 重新进入论文导读显示论文库而非上次搜索结果
    Given 用户已进入论文导读模式
    When 用户输入领域关键词“铝电解”
    And 点击搜索按钮
    And 用户重新进入论文导读模式
    Then 不显示上次的搜索结果
    And 仍显示搜索表单

  Scenario: 论文库按领域分组显示已缓存论文
    Given 用户已进入论文导读模式
    And 论文库中有“铝电解”领域的 2 篇论文和“本体论”领域的 1 篇论文
    When 用户查看论文导读欢迎页
    Then 论文库显示“铝电解”领域卡片且有 2 篇
    And 论文库显示“本体论”领域卡片且有 1 篇

  Scenario: 从论文库打开论文进入阅读
    Given 用户已进入论文导读模式
    And 论文库中有“铝电解”领域的 2 篇论文和“本体论”领域的 1 篇论文
    When 用户查看论文导读欢迎页
    And 用户展开“铝电解”领域
    And 用户点击论文库中第 1 篇论文的打开按钮
    Then 论文以 tab 形式打开

  Scenario: 缓存时条目显示加载状态且按钮禁用
    Given 用户已进入论文导读模式
    When 用户输入领域关键词“铝电解”
    And 点击搜索按钮
    And 用户点击第 1 条结果的缓存按钮
    Then 第 1 条结果的缓存按钮显示缓存中且被禁用
    And 进入缓存进度状态

  Scenario: 批量缓存失败逐条列出原因并给出逃生门
    Given 用户已进入论文导读模式
    When 用户输入领域关键词“铝电解”
    And 点击搜索按钮
    And 设定导入将因无开放获取而失败
    And 用户勾选第 1 和第 2 条结果
    And 用户点击批量缓存按钮
    Then 批量错误列表包含 2 条失败原因
    And 每条失败原因附带浏览器打开按钮

  Scenario: 已缓存论文可打开所在文件夹
    Given 用户已进入论文导读模式
    And 论文“https://arxiv.org/abs/2402.70007”已缓存
    When 用户输入领域关键词“铝电解”
    And 点击搜索按钮
    And 用户点击第 1 条结果的所在文件夹按钮
    Then 调用 show_in_folder 展示该论文文件

  Scenario: 设置面板显示实际论文目录
    Given 用户已打开设置面板
    Then 论文服务分组显示当前实际论文目录
    And 论文库根目录行包含打开文件夹按钮
